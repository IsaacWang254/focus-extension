name = "focus-extension-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"

# Public env vars — generated by scripts/build-local.js from .env
[vars]
TODOIST_CLIENT_ID = "__TODOIST_CLIENT_ID__"

# Secret env vars — set via CLI, never committed:
#   npx wrangler secret put TODOIST_CLIENT_SECRET
